{
  "name": "luker-video-toolkit",
  "version": "0.3.0",
  "description": "Turn-scoped HTTPS and GCS video attachments with configurable remote import workers for Luker.",
  "type": "module",
  "main": "server/index.mjs",
  "license": "AGPL-3.0-only",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kj1534/luker-video-toolkit.git"
  }
}
